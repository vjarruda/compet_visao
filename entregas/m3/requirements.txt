# PneumoVision — dependências iniciais
# Python 3.10+ recomendado
# Instale com: pip install -r requirements.txt
# Versões fixadas em faixas estáveis; ajuste conforme necessário.

# --- Visão computacional / extração de keypoints ---
mediapipe>=0.10,<0.11
opencv-python>=4.9,<5.0

# --- Computação numérica e dados ---
numpy>=1.26,<2.1
pandas>=2.1,<2.3
scipy>=1.11,<1.14

# --- Aprendizado de máquina clássico ---
scikit-learn>=1.4,<1.6
joblib>=1.3,<1.5          # serialização de modelos

# --- Visualização (análise/validação) ---
matplotlib>=3.8,<3.10
seaborn>=0.13,<0.14

# --- API / serviço (painel e feedback) ---
fastapi>=0.110,<0.116
uvicorn[standard]>=0.29,<0.31
pydantic>=2.6,<3.0

# --- Notebooks / exploração ---
jupyter>=1.0,<2.0

# --- Qualidade de código e testes ---
pytest>=8.0,<9.0
black>=24.3
ruff>=0.4

# --- Utilidades ---
tqdm>=4.66,<5.0          # barras de progresso
python-dotenv>=1.0,<2.0  # variáveis de ambiente
